/** Snappy UI transitions for overlays, modals, and drawers */
export const overlayTransition = { duration: 0.15, ease: [0.22, 1, 0.36, 1] as const };

export const modalTransition = { duration: 0.2, ease: [0.22, 1, 0.36, 1] as const };

export const drawerTransition = { duration: 0.22, ease: [0.22, 1, 0.36, 1] as const };
